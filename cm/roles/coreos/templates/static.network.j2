[Match]
Name={{ ansible_default_ipv4.interface }}

[Network]
Address={{ ansible_default_ipv4.address }}/{{ subnet }}
Address={{ hetzner_cloud_floating_ip }}/{{ subnet }}
Gateway={{ ansible_default_ipv4.gateway }}
